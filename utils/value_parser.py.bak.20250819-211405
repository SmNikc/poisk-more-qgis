from typing import Optional
class ValueParser:
    @staticmethod
    def try_parse_float(s: str) -> Optional[float]:
        try:
            return float(s)
        except ValueError:
            return None