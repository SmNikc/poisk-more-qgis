class SitrepDialog(QDialog): def init(self, iface): super().init() self.iface = iface layout = QVBoxLayout(self) self.sitrep_text = QLineEdit() layout.addWidget(QLabel("SITREP Text:")) layout.addWidget(self.sitrep_text) btn = QPushButton("Generate PDF") btn.clicked.connect(self.generate_pdf) layout.addWidget(btn)
def generate_pdf(self): c = canvas.Canvas("sitrep.pdf") c.drawString(100, 750, self.sitrep_text.text()) c.save() QMessageBox.information(self, "Сохранено", "SITREP PDF сгенерирован")